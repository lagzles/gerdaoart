<h2> Index images </h3>

<%- if logged?%>
  <%= link_to 'nova imagem', new_image_path%>
  <table>
    <thead>
      <tr>
        <th>Nome</th>
        <th>Categoria</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @images.each do |image| %>
        <tr>
          <td><%= link_to image.title, image_path(image)  %></td>
          <td><%= image.category.name %></td>
          <td><%= link_to 'add images', new_image_carousel_path(image) %></td>
          |
          <td><%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>


<%- @images.each do |image| %>
  <div class='row'>
    <br>
    <p><%= image.title%></p>
    <br>
    <%- image.carousels do |carousel| %>
      <div class='col-lg-6 col-md-6 col-sm-12'>
        <%= link_to image_path(carousel.image) do %>
          <%= image_tag carousel.url.url(:thumb)%>
          <div class="carousel-caption">
            <h2><%= carousel.name %></h2>
          </div>
        <% end %>
      </div>
    <%end%>
  </div>
  <br>
<% end %>

<%= render 'partials/carousel'%>
