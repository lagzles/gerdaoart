<%= simple_form_for @image , html: {class: 'form-horizontal'} do |f| %>
  <%= f.error_notification%>
  <%= f.input :url, as: :file %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :tags %>

  <%= f.button :submit %>

<% end %>
