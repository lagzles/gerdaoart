<%= form_tag(access_attempt_login_path, :method => 'post')%>
  <%= label_tag :username%>
  <%= text_field_tag :username, params[:username]%>
  <%= label_tag :password%>
  <%= password_field_tag :password, params[:password]%>
  <br>

  <%= submit_tag%>
