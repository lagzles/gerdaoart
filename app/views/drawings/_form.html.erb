<%= simple_form_for @drawing , html: {class: 'form-horizontal'} do |f| %>
  <%= f.error_notification%>
  <%= f.input :title %>

  <%= f.input :category_id, :collection => Category.all, :label_method => :name, :value_method => :id,
  :label => 'Categoria'%>

  <%= text_field_tag :category %>

  <%= f.button :submit %>

<% end %>
