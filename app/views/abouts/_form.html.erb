<%= simple_form_for about , html: {class: 'form-horizontal'} do |f| %>
  <%= f.error_notification%>
  <%= f.input :body, as: :text %>
  <%= f.input :pinned, label:"Fixar no Topo"%>

  <%= f.button :submit %>

<% end %>
